services:
  sweet-home-page:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sweet-home-page
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3001
      - DATA_DIR=/app/data
    restart: unless-stopped
